{# gdev-content/user_template.j2 #}
{# version: v1.0.0 #}

Generate a structured support output for this ticket.

Context:
- mode: "{{ mode }}"
- lang: "{{ lang }}"

Ticket content is appended below as raw text between explicit delimiters.
